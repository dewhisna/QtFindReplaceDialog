readSettings writeSettings (with dialog current status)

history of searched text

Find Next/ Find Previous

example with embedding of the forms in a dock?

on-the-fly search (highlight all occurrences)

qtfindreplacedialog.* can be removed

finddialog and findform as derived from findreplace (removing
replace specific widgets)